import { state } from '../../app/state.js'
export function renderHorizonForm() {
  return `<form data-planning-horizon><h3>Horizonte do planejamento</h3><p>A idade-alvo define o fim do fluxo de caixa, não o início da aposentadoria. O horizonte inclui dezembro do ano em que essa idade é atingida. Como não há data de nascimento cadastrada, o ano é estimado pela idade atual no mês de referência.</p><div class="form-grid form-grid--two"><label class="form-field"><span>Idade-alvo do planejamento</span><input name="targetAge" type="number" min="${state.plan.currentAge + 1}" max="110" step="1" value="${state.plan.targetAge ?? ''}" required /></label><label class="form-field"><span>Mês de referência da idade atual (${state.plan.currentAge} anos)</span><input name="reference" type="month" min="2000-01" max="2199-12" value="${state.plan.horizonReferenceMonth || new Date().toISOString().slice(0, 7)}" required /></label></div><button class="button button--secondary">Salvar horizonte</button></form>`
}
